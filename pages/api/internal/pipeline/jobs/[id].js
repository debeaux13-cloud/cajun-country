import{head}from'@vercel/blob';
function auth(req){const s=process.env.MCS_WORKER_SECRET||'';const h=req.headers.authorization||'';return !!s&&(h==='Bearer '+s||h===s)}
async function loadStagePlan(id){const token=process.env.BLOB_READ_WRITE_TOKEN;if(!token)throw new Error('Blob storage missing');const meta=await head(`mcs/jobs/${id}/story-plan.bin`,{token});const r=await fetch(meta.downloadUrl||meta.url,{headers:{Authorization:`Bearer ${token}`}});if(!r.ok)throw new Error(`Stage plan fetch failed ${r.status}`);const saved=JSON.parse(await r.text());return String(saved.plan||'').trim()}
function storyScenes(plan){let lines=String(plan||'').split(/\n+/).map(x=>x.trim()).filter(Boolean).map(x=>x.replace(/^\s*\d+[.)-]?\s*/,''));if(lines.length<18)lines=String(plan||'').split(/(?<=[.!?])\s+/).map(x=>x.trim()).filter(Boolean);if(lines.length<18)throw new Error(`Stage plan contains ${lines.length} usable scenes; expected 18`);return lines.slice(0,18).map((text,i)=>{const n=i+1;return{sceneNumber:n,text,description:text,characters:['Main Character'],setting:`Story scene ${n}: ${text.slice(0,140)}`,emotionalTone:n<=6?'building attachment':n<15?'adventurous':'emotional payoff',keyActionVerbs:['moves','reacts','interacts','advances'],narration:text,visibleAction:`The main character visibly performs exactly this story beat: ${text}`,requiredVisibleDetails:['preserve exact reference identity','show every narrated object/action','continuous purposeful character motion','cinematic animated-feature style, not photoreal and not flat kid-cartoon'],motionBeats:[`0-3 seconds: begin the narrated action immediately`,`3-7 seconds: travel, react, and interact through this exact story beat`,`7-10 seconds: visibly complete this narrated beat and set up the next scene`],emotionalIntensity:Math.min(10,4+Math.ceil(n/3)),actionDensity:8,staticLevel:0,petPresent:true,hero_scene:true,pet_present:true,animationProvider:'runway-gen4-turbo'}})}
async function contract(id){const base='https://main-character-studios.vercel.app/api/internal/pipeline/jobs/'+id;const runway=process.env.Runway||process.env.RUNWAY_API_KEY||'';const eleven=process.env.ELEVENLABS_API_KEY||process.env.Elevenlabs_Secured_key||process.env.Elevenlabs_Secured_key_2||'';const plan=await loadStagePlan(id);const scenes=storyScenes(plan);return{id,orderId:id,tier:'standard_hybrid',vision:plan,approvedPreview:{title:'Main Character Studios Preview',page_title:'The story begins',story_text:plan,scene_prompt:plan,organized_story:plan,original_customer_input:plan,selected_topic:'',featured_names:['Main Character'],subject_kind:'pet_or_mixed',character_traits:{subject_count:1,subject_type:'pet_or_person',species:'unknown',breed:'unknown',traits:[],hard_constraints:[],ambiguity_notes:[],customer_notes:''},character_notes:''},ownerQa:false,retryLimitHit:0,resumeStoredAssets:true,reusableOwnerSceneNumbers:[1,2,3,4,5,6],reusableOwnerVideoSceneNumbers:[1,2,3,4,5,6],existingProviderJobs:{},existingManifest:{version:3,tier:'three_minute',title:'Main Character Studios Movie',subtitle:'Three-minute personalized moving story',pages:scenes.map(s=>({sceneNumber:s.sceneNumber,text:s.text})),scenes},assets:{reference:base+'/asset?kind=reference',approvedScene:base+'/asset?kind=approved-scene',previewScene:base+'/asset',upload:base+'/asset',illustrate:base+'/illustrate'},callback:base,rankManifest:base+'/rank',providers:{openaiApiKey:'',runwayApiKey:runway,elevenLabsApiKey:eleven,elevenLabsVoiceId:process.env.ELEVENLABS_VOICE_ID||'21m00Tcm4TlvDq8ikWAM',elevenLabsModelId:process.env.ELEVENLABS_MODEL_ID||'eleven_flash_v2_5'},contract:{scenes:18,previewScenes:6,secondsPerScene:10,previewSeconds:60,movieSeconds:180,heroScenes:18,heroScoring:false,motion:'runway-gen4-turbo-all-scenes',petMode:true,petRouting:'runway-gen4-turbo-only',animalPoseDetection:false}}}
export default async function handler(req,res){if(!auth(req))return res.status(401).send('Unauthorized');const{id}=req.query;if(req.method==='GET'){try{return res.status(200).json(await contract(id))}catch(e){return res.status(500).json({error:e.message})}}if(req.method==='PATCH')return res.status(200).json({ok:true,id,...(req.body||{})});return res.status(405).json({error:'Method not allowed'})}