function auth(req){const s=process.env.MCS_WORKER_SECRET||'';const h=req.headers.authorization||'';return !!s&&(h==='Bearer '+s||h===s)}const memory=new Map();export default async function handler(req,res){if(!auth(req))return res.status(401).send('Unauthorized');const{id,kind='preview'}=req.query;const key=id+':'+kind;if(req.method==='PUT'){const chunks=[];for await(const c of req)chunks.push(c);memory.set(key,Buffer.concat(chunks));return res.status(200).json({ok:true,id,kind})}if(req.method==='GET'){const data=memory.get(key);if(!data)return res.status(404).json({error:'asset not found'});res.setHeader('Content-Type','application/octet-stream');return res.status(200).send(data)}return res.status(405).json({error:'Method not allowed'})}export const config={api:{bodyParser:false}}