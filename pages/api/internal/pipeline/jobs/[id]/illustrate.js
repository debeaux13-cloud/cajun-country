function auth(req){const s=process.env.MCS_WORKER_SECRET||'';const h=req.headers.authorization||'';return !!s&&(h==='Bearer '+s||h===s)}export default async function handler(req,res){if(!auth(req))return res.status(401).send('Unauthorized');if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const runway=process.env.Runway||process.env.RUNWAY_API_KEY||'';if(!runway)return res.status(503).json({error:'Runway credential missing'});return res.status(200).json({ok:true,provider:'runway',accepted:true})}