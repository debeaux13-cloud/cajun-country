import{head}from'@vercel/blob';
function auth(req){const s=process.env.MCS_WORKER_SECRET||'';const h=req.headers.authorization||'';return !!s&&(h==='Bearer '+s||h===s)}
async function loadStory(id){const token=process.env.BLOB_READ_WRITE_TOKEN;if(!token)throw new Error('Blob storage missing');const path=`mcs/jobs/${id}/story-plan.bin`;const meta=await head(path,{token});const r=await fetch(meta.downloadUrl||meta.url,{headers:{Authorization:`Bearer ${token}`}});if(!r.ok)throw new Error(`Story fetch failed ${r.status}`);const saved=JSON.parse(await r.text());if(!Array.isArray(saved?.screenplay?.scenes)||saved.screenplay.scenes.length!==18)throw new Error('Structured screenplay missing');return saved}
function previewScenes(saved){return saved.screenplay.scenes.slice(0,6).map((s,i)=>({...s,sceneNumber:i+1,staticLevel:0,hero_scene:true,animationProvider:'runway-gen4-turbo'}))}
async function contract(id){const origin='https://main-character-studios.vercel.app';const paidAsset=`${origin}/api/internal/pipeline/jobs/${id}/asset`;const runway=process.env.Runway||process.env.RUNWAY_API_KEY||'';const eleven=process.env.ELEVENLABS_API_KEY||process.env.Elevenlabs_Secured_key||process.env.Elevenlabs_Secured_key_2||'';const saved=await loadStory(id);const scenes=previewScenes(saved);const title=saved.screenplay.title||'Main Character Studios Preview';return{id,previewId:id,tier:'standard_hybrid',vision:saved.plan||'',approvedPreview:{title,organized_story:saved.plan||'',featured_names:scenes[0]?.characters||[],character_traits:{subject_count:1,subject_type:'pet_or_person',traits:[],hard_constraints:['preserve exact identity and anatomy'],customer_notes:''}},existingManifest:{version:4,title,subtitle:'The opening minute',pages:scenes.map(s=>({sceneNumber:s.sceneNumber,text:s.narration})),scenes},existingProviderJobs:{},assets:{reference:`${paidAsset}?kind=reference`,previewScene:paidAsset,upload:paidAsset},providers:{openaiApiKey:'',runwayApiKey:runway,elevenLabsApiKey:eleven,elevenLabsVoiceId:process.env.ELEVENLABS_VOICE_ID||'21m00Tcm4TlvDq8ikWAM',elevenLabsModelId:process.env.ELEVENLABS_MODEL_ID||'eleven_flash_v2_5'},contract:{scenes:6,previewScenes:6,secondsPerScene:10,previewSeconds:60,movieSeconds:60,fullMovieScenes:18,fullMovieSeconds:180,petRouting:'runway-gen4-turbo-only',animalPoseDetection:false}}}
export default async function handler(req,res){if(!auth(req))return res.status(401).send('Unauthorized');const{id}=req.query;if(req.method==='GET'){try{return res.status(200).json(await contract(id))}catch(e){return res.status(500).json({error:e.message})}}if(req.method==='PATCH')return res.status(200).json({ok:true,id,...(req.body||{})});return res.status(405).json({error:'Method not allowed'})}