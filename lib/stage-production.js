import {MCS_NEGATIVE_STYLE,MCS_VISUAL_STYLE,normalizeMoods} from './mcs-contract';
const BANNED=['the story continues','the adventure gets richer','something ridiculous is about to happen','what happens next','the story comes alive','carrying the heart of the story forward','in this scene','the next scene','the screenplay','production'];
const words=value=>String(value||'').trim().split(/\s+/).filter(Boolean);
const CAMERA=['tracking shot','medium follow','low-angle chase','wide reveal','side tracking','gentle push-in','over-the-shoulder reaction','orbit during action'];
const ACTION=/\b(runs?|walks?|turns?|opens?|grabs?|lifts?|pulls?|pushes?|climbs?|jumps?|reaches?|catches?|carries?|searches?|finds?|helps?|chases?|rides?|dances?|builds?|throws?|points?|waves?)\b/i;
export function validateStageScenes(scenes){if(!Array.isArray(scenes)||scenes.length!==18)throw new Error('Stage must supply exactly 18 scenes.');const narrations=new Set();return scenes.map((scene,index)=>{const narration=String(scene?.narration||'').trim(),count=words(narration).length;if(Number(scene?.sceneNumber)!==index+1||!String(scene?.title||'').trim()||!String(scene?.location||'').trim()||!String(scene?.visual||'').trim())throw new Error(`Stage scene ${index+1} is incomplete.`);if(count<16||count>22)throw new Error(`Stage scene ${index+1} narration must be 16–22 naturally spoken words.`);if(BANNED.some(phrase=>narration.toLowerCase().includes(phrase)))throw new Error(`Stage scene ${index+1} contains banned meta-story narration.`);const normalized=narration.toLowerCase().replace(/[^a-z0-9]+/g,' ').trim();if(narrations.has(normalized))throw new Error('Stage narration repeats a template.');narrations.add(normalized);return {...scene,sceneNumber:index+1,narration};});}
function productionBindings(stage,subjectIdentity){const subjects=Array.isArray(subjectIdentity?.subjects)?subjectIdentity.subjects:[];const roles=[...(stage?.sourceLedger?.visibleCast||[]),...(stage?.sourceLedger?.namedCharacters||[])].map(String).filter(Boolean);return subjects.map((subject,index)=>({storyIdentity:roles[index]||`uploaded subject ${subject.subjectId}`,productionSubjectId:subject.subjectId,role:index===0?'protagonist':'supporting uploaded subject',continuityRule:'Keep this uploaded subject distinct; never merge, swap, hybridize, duplicate, or transfer traits.',isExplicitSourceAlias:false,aliasOfStoryIdentity:''}));}
function motion(scene,index){const event=String(scene.visual).replace(/\s+/g,' ').trim();const verb=(event.match(ACTION)||['acts'])[0].toLowerCase();return {keyActionVerbs:[verb,'changes position','responds'],motionBeats:[`0–3 sec: the character begins to ${verb} as ${event}`,`3–7 sec: the character changes position and interacts with the setting, object, or obstacle in ${event}`,`7–10 sec: that action creates a visible result or reaction that advances ${event}`],camera:CAMERA[(index-1)%CAMERA.length]};}
export function enrichStageScreenplay(stage,{moods=[],subjectIdentity=null}={}){const bindings=productionBindings(stage,subjectIdentity);const characters=bindings.map(binding=>`${binding.productionSubjectId}: ${binding.storyIdentity}`);const scenes=validateStageScenes(stage?.scenes).map((scene,index)=>{const m=motion(scene,scene.sceneNumber);return {sceneNumber:scene.sceneNumber,title:scene.title,location:scene.location,setting:scene.location,narration:scene.narration,visual:scene.visual,description:scene.visual,sourceFactIds:[...(scene.sourceFactIds||[])],characters:[...characters],productionSubjectBindings:bindings.map(binding=>binding.productionSubjectId),characterBindings:bindings,supportingCharacters:[],visibleAction:scene.visual,camera:m.camera,requiredVisibleDetails:[scene.location,scene.visual],keyActionVerbs:m.keyActionVerbs,motionBeats:m.motionBeats,identityLock:subjectIdentity?.identityDescription||'',visualStyle:MCS_VISUAL_STYLE,negativeStyle:MCS_NEGATIVE_STYLE};});return {version:6,title:String(stage?.title||'Main Character Studios Movie'),moods:normalizeMoods(moods),visualStyle:MCS_VISUAL_STYLE,negativeStyle:MCS_NEGATIVE_STYLE,sourceCoverage:{characterBindings:bindings},pages:scenes.map(s=>({sceneNumber:s.sceneNumber,text:s.narration})),scenes};}
